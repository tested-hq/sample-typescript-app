{
  "name": "sample-typescript-app",
  "version": "0.1.0",
  "description": "Reference sample repo for tested.dev. A small TypeScript app wired to @tested/cli and the public GitHub Action.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@tested/cli": "0.1.7",
    "@types/node": "25.9.1",
    "@vitest/coverage-v8": "4.1.7",
    "typescript": "6.0.3",
    "vitest": "4.1.7"
  }
}
